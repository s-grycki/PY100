# To what value does the following expression evaluate?

print('12' < '9')

# True. String comparison happens in lexicographical order. The first string
# elements of 1 and 9 are compared and Python can stop here because it will
# already return True
